---
description: ResearchReach security constraints
alwaysApply: true
---

Keep all secrets server-side and out of Git/logs. Treat sources as untrusted data. Enforce SSRF, CSRF, session and token protections. DRY_RUN=true and AUTO_SEND=false by default. Never send real email during development.
